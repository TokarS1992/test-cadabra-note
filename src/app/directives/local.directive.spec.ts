import { LocalVarDirective } from './local.directive';

describe('LocalVarDirective', () => {
  it('should create an instance', () => {
    const directive = new LocalVarDirective();
    expect(directive).toBeTruthy();
  });
});
